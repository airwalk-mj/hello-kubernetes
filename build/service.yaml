apiVersion: v1
kind: Service
metadata:
  name: ${app_name}
  namespace: hello
spec:
  selector:
    app: ${app_name}  # ties this service in with the pods
  ports:
    - name: http
      protocol: TCP
      port: 80          # container port
      targetPort: 80    # service port
